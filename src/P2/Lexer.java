package P2;

public class Lexer {
	/*
	 * Pattern for lexemes for the other tokens
	 * INT = [0-9]+
	 * ID = [A-Za-z]+
	 * END = end
	 * PRINT = print
	 */
	
	
	public void nextToken() {
		//TODO: scan standart input, look for patterns
		// that match one of the tokens
		// return TokenCode = ERROR if some illegal lexems are found
	}

}
